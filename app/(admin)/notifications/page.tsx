import SectionPlaceholder from "@/components/SectionPlaceholder";

export default function NotificationsPage() {
  return (
    <SectionPlaceholder
      eyebrow="Notifications"
      title="Notifications"
      description="Control alert preferences for uploads, indexing, conversations, and agent events."
    />
  );
}

import SectionPlaceholder from "@/components/SectionPlaceholder";

export default function AppSettingsPage() {
  return (
    <SectionPlaceholder
      eyebrow="App settings"
      title="App Settings"
      description="Configure workspace-wide defaults, appearance, and admin behavior."
    />
  );
}
